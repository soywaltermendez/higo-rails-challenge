= form_with(model: invoice, local: true) do |form|
  - if invoice.errors.any?
    #error_explanation
      %h2
        = pluralize(invoice.errors.count, "error")
        bugs found:
      %ul
        - invoice.errors.full_messages.each do |message|
          %li= message
  .row
    .form-group.files.color
      = form.label :xmls, "Upload your xml files", class: "sr-only", for: "xmls"
      = form.file_field :xmls, name: "xmls[]", multiple: true, type: "file", accept: "text/xml", class: "form-control"
  .actions.pt-3
    = link_to "Cancel", invoices_path, class: "btn btn-secondary"
    = form.submit "Save", class: "btn btn-primary"